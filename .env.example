# Copy to .env and adjust. Without a .env the tests build the app and test it locally.
#
# Where the app under test runs. When set, Playwright does not start a local server
# but points the suite at this address (a test or acceptance environment, for example).
# BASE_URL=https://test.example.com
